<?php

/**
 * Register some global functions.
 */

if ( ! function_exists( 'untappd_remote_get' ) ) {
	function untappd_remote_get( $args = array() ) {
		// TODO
	}
}

if (  ! function_exists( 'untappd_remote_post' ) ) {
	function untappd_remote_post( $args = array() ) {
		// TODO
	}
}
